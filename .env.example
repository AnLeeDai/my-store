# Turso Database Configuration (for production)
TURSO_AUTH_TOKEN="your_turso_auth_token_here"
TURSO_DATABASE_URL="libsql://your-database-url.turso.io"

# Environment configuration
NODE_ENV="development"

# Database configuration
# Local development uses SQLite file
DATABASE_URL="file:./dev.db"

# Production uses Turso (configured via environment variables on Vercel)

# Setup secret for production database initialization
SETUP_SECRET="your_secure_setup_secret_here"
